<h2>New Tweet!</h2>
<form class="tweet-compose" data-tweets-ul="#feed" action="/tweets" method="POST">
  <input
      type="hidden"
      name="authenticity_token"
      value="<%= form_authenticity_token %>">

  <label>
    Content
    <textarea rows=8 cols=40 name="tweet[content]"></textarea>
  </label>
  <br>

  <strong class="chars-left"></strong>

  <br>

  <label>
    Mention

    <script type="text/template">
      <div>
        <select name="tweet[mentioned_user_ids][]">
          <option></option>
          <% User.all.each do |user| %>
            <option value="<%= user.id %>"><%= user.username %></option>
          <% end %>
        </select>
        <a href="#" class="remove-mentioned-user">Remove mention</a>
      </div>
    </script>

  </label>
  <br>

  <a href="#" class="add-mentioned-user">Add mention</a>
  <br>

  <div class="mentioned-users">

  </div>
  <br>

  <input type="Submit" value="Post Tweet!">
</form>
